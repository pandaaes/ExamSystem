server:
  port: 8000
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/exam-system?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123456

  devtools:
    restart:
      enabled: true  #???????
      additional-paths: src/main/java #????
      exclude: WEB-INF/**
  freemarker:
    cache: false

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true